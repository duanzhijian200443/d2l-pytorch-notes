name: Sync study log 6.3

on:
  push:
    branches:
      - main
    paths:
      - .github/workflows/sync-study-log-6-3.yml

permissions:
  contents: write

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Smoke test
        run: echo ok
